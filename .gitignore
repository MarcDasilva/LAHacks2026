.venv/
__pycache__/
*.pyc
*.egg-info/
.DS_Store
data/
database/data/
database/.venv/
/9jYiR3Yltzb/
/mobile/whisper-tiny/

# Sparse-splat pipeline outputs — large binaries kept local-only.
# The .lbmp + URL pointer JSONs are committed; the source video copy isn't.
dashboard/public/clouds/video.mp4
# COLMAP intermediates under assets/output (input video, frames, db, sparse model).
assets/output/database.db
assets/output/images/
assets/output/sparse/
assets/output/logs/

# lingbot_bridge runtime artifacts (model weights, captures, derived data)
lingbot_bridge/models/
lingbot_bridge/vendor/
lingbot_bridge/outputs/
lingbot_bridge/state/
lingbot_bridge/frames/*/
# macOS
.DS_Store

# Secrets / local env
.env
*.env.local

# Xcode user-specific data
**/xcuserdata/
**/*.xcuserstate
**/*.xcscmblueprint

# Build artifacts
DerivedData/
build/
.build/

# SwiftPM local caches
.swiftpm/

# Fastlane / tooling outputs (if added later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

.env

assets/output/images/

venv/
assets/output/input/
